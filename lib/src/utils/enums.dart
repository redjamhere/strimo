// Описание перечеслений

enum StreamMemberType { single, group }
enum StreamType { standard, planning, requested }
enum ChatType { single, group, stream }
enum MessageType { message, image, voice, requestMessage, location, video, none }
enum SocialAuthType {google, facebook, apple}
enum Sex { male, female, other }
